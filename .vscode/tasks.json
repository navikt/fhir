{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run IG Publisher",
      "type": "shell",
      "command": "docker run -it --rm -v ${workspaceFolder}/igs/MessagingCore:/data navikt/fhir-ig-dev navikt/fhir-ig-dev -ig /data/ig.ini",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}